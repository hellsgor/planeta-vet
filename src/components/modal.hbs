<div class="modal modal_hidden modal_{{this.name}}" id="modal-{{this.name}}" data-modal-name="{{this.name}}">
  <button class="modal__close-button" type="button">
    <svg>
      <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#close" />
    </svg>
  </button>
  
  {{#if (eq this.name 'thank-you')}}{{> modals/thank-you}}{{/if}}
  {{#if (eq this.name 'burger-menu')}}{{> modals/burger-menu }}{{/if}}     
  {{#if (eq this.name 'services-bubble')}}{{> modals/services-bubble }}{{/if}}     
  {{#if (eq this.name 'cooperation')}}{{> modals/cooperation }}{{/if}}     
  {{#if (eq this.name 'entrance')}}{{> modals/entrance }}{{/if}}     
  {{#if (eq this.name 'forgot')}}{{> modals/forgot }}{{/if}}     
</div>
