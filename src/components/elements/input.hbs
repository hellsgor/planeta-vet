<div class="form-control{{#if this.classes}} {{this.classes}}{{/if}}">
<input 
  class="form-control__input"
  type="text" 
  {{#if this.placeholder}} placeholder="{{this.placeholder}}"{{/if}}
  {{#if this.id}} id="{{this.id}}"{{/if}}
  {{#if this.name}} name="{{this.name}}"{{/if}}
>
<p class="form-control__error">{{#if this.error}} {{this.error}}{{/if}}</p>
</div>
